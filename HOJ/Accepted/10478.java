import java.util.*;

public class j10478 
{
	static int num,maxSize=0;
	public static void main(String[] args) 
	{
		Scanner cin = new Scanner(System.in);
		num=cin.nextInt();
		int[] source,temp,left,right;
		if(num>maxSize)
		{
			source=new int[num];
			temp=new int[num];
			left=new int[num];
			right=new int[num];
			System.gc();
		}
		for(int i=0;i<num;i++)
			source[i]=cin.nextInt();
		int amax;amax=temp[0]=source[0];
		for(int i=1;i<num;i++)
		{
			;
		}
	}
}
